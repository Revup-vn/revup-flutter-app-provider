import 'package:flutter/material.dart';

import '../widgets/calling_view.u.dart';

class CallingPage extends StatelessWidget {
  const CallingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const CallingView();
  }
}
